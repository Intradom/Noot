using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Effect_Collision_Knockback : MonoBehaviour
{
    /*
    [SerializeField] private string[] tag_applicable;
    [SerializeField] private float vel_multiplier = 0f;
    [SerializeField] private float return_speed = 0f;

    private Vector3 ref_pos_starting;

    private void Start()
    {
        ref_pos_starting = transform.position;
    }

    private void Update()
    {
        if (transform.position != ref_pos_starting)
        {

        }
    }

    private void OnCollisionEnter2D(Collision2D collision)
    {
        bool applicable = false;
        foreach (string t in tag_applicable)
        {
            if (t == collision.collider.tag)
            {
                applicable = true;
            }
        }

        if (!applicable)
        {
            return;
        }

        float hit_mag = collision.collider.attachedRigidbody.velocity.magnitude;
        Vector3 collider_pos = collision.collider.transform.position;

        
    }
    */
}
